
// debug amounts of log output
setLogMode(6);
// run game from the script files.  don't compile them first
$Scripts::ignoreDSOs = true;
setRandomSeed();
ModuleDatabase.scanModules( "modules" );
ModuleDatabase.LoadExplicit( "games" );

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//onExit
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

function onExit()
{
  ModuleDatabase.unloadExplicit( "games" );
}

/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
//androidBackButton
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

function androidBackButton(%val)
{
  if (%val)
  {
    //Add code here for other options the back button can do like going back a screen.  the quit should happen at your main menu.

    quit();
  }
}
